name: Refresh viva.m3u Playlist

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/update_viva.yml'

jobs:
  update-playlist:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run Script
        run: python update.py

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add viva.m3u
          git commit -m "Auto-refresh viva.m3u [skip ci]" || echo "No changes to commit"
          git push origin main

              for line in lines:
                  stripped = line.strip()
                  if not stripped or stripped.startswith("#EXTM3U"):
                      continue

                  if stripped.startswith("#EXTINF:"):
                      if current_entry:
                          formatted = format_entry(current_entry)
                          if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                              formatted_entries.append(formatted)
                      current_entry = [stripped]
                  elif current_entry:
                      current_entry.append(stripped)

              if current_entry:
                  formatted = format_entry(current_entry)
                  if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                      formatted_entries.append(formatted)

              return formatted_entries

          def main():
              print("Downloading source playlist...")
              raw_m3u = fetch_playlist(SOURCE_URL)

              print("Formatting channel entries...")
              entries = process_playlist(raw_m3u)
              print(f"Total processed channels: {len(entries)}")

              with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
                  f.write("#EXTM3U\n")
                  for item in entries:
                      f.write(item + "\n")

              print(f"{OUTPUT_FILE} successfully written.")

          if __name__ == "__main__":
              main()
          EOF

      - name: Commit and Push Updated viva.m3u
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add viva.m3u
          if git diff --staged --quiet; then
            echo "No changes in viva.m3u"
          else
            git commit -m "Auto-refresh viva.m3u [skip ci]"
            git push origin main
          fi
              for line in lines:
                  stripped = line.strip()
                  if not stripped or stripped.startswith("#EXTM3U"):
                      continue

                  if stripped.startswith("#EXTINF:"):
                      if current_entry:
                          formatted = format_entry(current_entry)
                          if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                              formatted_entries.append(formatted)
                      current_entry = [stripped]
                  elif current_entry:
                      current_entry.append(stripped)

              if current_entry:
                  formatted = format_entry(current_entry)
                  if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                      formatted_entries.append(formatted)

              return formatted_entries

          def main():
              print("Downloading source playlist...")
              raw_m3u = fetch_playlist(SOURCE_URL)

              print("Formatting channel entries...")
              entries = process_playlist(raw_m3u)
              print(f"Total processed channels: {len(entries)}")

              with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
                  f.write("#EXTM3U\n")
                  for item in entries:
                      f.write(item + "\n")

              print(f"{OUTPUT_FILE} successfully written.")

          if __name__ == "__main__":
              main()
          EOF

      - name: Commit and Push Updated viva.m3u
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add viva.m3u
          if git diff --staged --quiet; then
            echo "No changes in viva.m3u"
          else
            git commit -m "Auto-refresh viva.m3u [skip ci]"
            git push origin main
          fi
              current_entry = []

              for line in lines:
                  stripped = line.strip()
                  if not stripped or stripped.startswith("#EXTM3U"):
                      continue

                  if stripped.startswith("#EXTINF:"):
                      if current_entry:
                          formatted = format_entry(current_entry)
                          if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                              formatted_entries.append(formatted)
                      current_entry = [stripped]
                  elif current_entry:
                      current_entry.append(stripped)

              if current_entry:
                  formatted = format_entry(current_entry)
                  if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                      formatted_entries.append(formatted)

              return formatted_entries

          def main():
              print("Downloading source playlist...")
              raw_m3u = fetch_playlist(SOURCE_URL)

              print("Formatting channel entries...")
              entries = process_playlist(raw_m3u)
              print(f"Total processed channels: {len(entries)}")

              with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
                  f.write("#EXTM3U\n")
                  for item in entries:
                      f.write(item + "\n")

              print(f"{OUTPUT_FILE} successfully written.")

          if __name__ == "__main__":
              main()
          EOF

      - name: Commit and Push Updated viva.m3u
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add viva.m3u
          git commit -m "Auto-refresh viva.m3u [skip ci]" || echo "No changes to commit"
          git push origin main
              current_entry = []

              for line in lines:
                  stripped = line.strip()
                  if not stripped or stripped.startswith("#EXTM3U"):
                      continue

                  if stripped.startswith("#EXTINF:"):
                      if current_entry:
                          formatted = format_entry(current_entry)
                          if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                              formatted_entries.append(formatted)
                      current_entry = [stripped]
                  elif current_entry:
                      current_entry.append(stripped)

              if current_entry:
                  formatted = format_entry(current_entry)
                  if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                      formatted_entries.append(formatted)

              return formatted_entries

          def main():
              print("Downloading source playlist...")
              raw_m3u = fetch_playlist(SOURCE_URL)

              print("Formatting channel entries...")
              entries = process_playlist(raw_m3u)
              print(f"Total processed channels: {len(entries)}")

              with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
                  f.write("#EXTM3U\n")
                  for item in entries:
                      f.write(item + "\n")

              print(f"{OUTPUT_FILE} successfully written.")

          if __name__ == "__main__":
              main()
          EOF

      - name: Commit and Push Updated viva.m3u
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add viva.m3u
          if git diff --staged --quiet; then
            echo "No changes detected in viva.m3u."
          else
            git commit -m "Auto-refresh viva.m3u [skip ci]"
            git push origin main
          fi
                  elif not stripped.startswith("#"):
                      url = stripped

              if url:
                  url = re.sub(r"&xxx=[^|]*", "", url)
                  url_base = url.split("|", 1)[0]

                  headers = []
                  if user_agent:
                      headers.append(f"User-Agent={user_agent}")
                  if cookie:
                      headers.append(f"Cookie={cookie}")

                  if headers:
                      url = f"{url_base}|{'&'.join(headers)}"
                  else:
                      url = url_base

              output = [extinf]
              output.append("#KODIPROP:inputstream.adaptive.license_type=clearkey")
              if hex_key:
                  output.append(f"#KODIPROP:inputstream.adaptive.license_key={hex_key}")
              if user_agent:
                  output.append(f"#EXTVLCOPT:http-user-agent={user_agent}")
              if url:
                  output.append(url)

              return "\n".join(output)

          def process_playlist(raw_content):
              lines = raw_content.splitlines()
              formatted_entries = []
              current_entry = []

              for line in lines:
                  stripped = line.strip()
                  if not stripped or stripped.startswith("#EXTM3U"):
                      continue

                  if stripped.startswith("#EXTINF:"):
                      if current_entry:
                          formatted = format_entry(current_entry)
                          if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                              formatted_entries.append(formatted)
                      current_entry = [stripped]
                  elif current_entry:
                      current_entry.append(stripped)

              if current_entry:
                  formatted = format_entry(current_entry)
                  if not FILTER_TAGS or any(tag.lower() in formatted.lower() for tag in FILTER_TAGS):
                      formatted_entries.append(formatted)

              return formatted_entries

          def main():
              print("Downloading source playlist...")
              raw_m3u = fetch_playlist(SOURCE_URL)

              print("Formatting channel entries...")
              entries = process_playlist(raw_m3u)
              print(f"Total processed channels: {len(entries)}")

              with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
                  f.write("#EXTM3U\n")
                  for item in entries:
                      f.write(item + "\n")

              print(f"{OUTPUT_FILE} successfully written.")

          if __name__ == "__main__":
              main()
          EOF

      - name: Commit and Push Updated viva.m3u
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add viva.m3u
          if git diff --staged --quiet; then
            echo "No changes detected in viva.m3u."
          else
            git commit -m "Auto-refresh viva.m3u [skip ci]"
            git push origin main
          fi
